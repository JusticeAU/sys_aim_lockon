/// @description 

//radius of auto-aim area around cursor
lock_radius = 50;

//hide the OS cursor
window_set_cursor(cr_none);

//create our target indicator
target = instance_create_layer(x,y,layer,oTarget);
